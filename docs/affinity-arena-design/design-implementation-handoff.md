# Affinity Arena implementation handoff

## Current candidate: v0.8, walk-up flow

See `v08-surface-brief.md` and `../AFFINITY-ARENA-V08-QUALITY.md`. Preserve the neon venue; the initial full selector is removed. Real instruments open the session, 21-second training ends in an attribute recap, and stage readiness requires walking to a microphone. New musical icons and synthesized leads/errors support input. Every menu minimizes, and secondary actions expand from Menu. 88 automated tests and SDK build pass; native 0.8 acceptance and deployment remain HOLD. The existing 0.7 World receipt is historical evidence, not a receipt for this revision.

## Historical v0.7 candidate, September 9 UTC

The user-directed nightclub rebuild supersedes the historical sections below. Read `v07-surface-brief.md`, `../AFFINITY-ARENA-V07-QUALITY.md` and the current native `decentraland/DESIGN.md` contract. The active entrypoint is `concert-scene.tsx`: four parcels, six themed studios, density-aware controls, illustrated atlas, disclosed bot exhibition, scored rehearsal and live performance weighted 2x. Typecheck, 84 automated tests and production build pass; native/mobile and World-update acceptance remain pending. The current sidecar documents native source only, without fabricated HTML specimens. The old no-raster statement below applies only to v0.5. BUIDL 48375 is already submitted and Under Review; do not repeat submission.

## Historical v0.6 candidate, September 9 UTC

This section supersedes the historical v0.5 source dimensions and publication boundaries below. See `../AFFINITY-ARENA-V06-QUALITY.md` for research, checks and remaining acceptance. v0.6 is local only: TypeScript, 61 tests and production build pass. Tests include the actual production controller running against an in-memory SDK adapter, not a native renderer or four real users.

The desk now anchors bottom-right, with available dimensions capped at 450×530, a 332 minimum available height, and actual heights 332 compact / 410 normal / 430 normal result. Notes are 20 pixels; fields are 48 compact / 108 normal. Lobby practice, truthful crew labels, authoritative phase cues, both crews' HP/energy/shield, a three-step guide, stage-only view and optional reduced motion are implemented. Opaque unlit geometry and change-gated stage writes reduce redundant component work without claiming a measured FPS gain.

The previous v0.5 World entity has completed conversion in Creator Hub. This does not publish v0.6 or establish public playability. Native listening, physical phone use, four-real-person gameplay, final thumbnail/video, DoraHacks and X receipts remain pending. The existing source-publication commit below is historical and excludes this candidate.

## Historical v0.5 extraction

The refreshed design system describes the implemented native SDK7 rhythm extension under `RHYTHM-BRIEF.md`. This documentation pass updates only DESIGN.md, its sidecar and the two handoff/reference documents in this folder. No code, HACKOPS data, prior Signal Grove design document or shipping asset was changed by this documentation pass.

| Evidence | Actual scope |
| --- | --- |
| Scene, layout, rhythm, pure rules, session transport and audio generator inspected by this documenter | Implemented values, components, state presentation, transport timing and original synthesis |
| 50 tests and production build PASS, reported by parent task | Source/build evidence only; not rerun by the documenter |
| Public source commit `ed316d6dbf3b17b68f660bd56a50e559d3760507`, remote verified by parent task | Source publication only; not World deployment, native runtime evidence or hackathon/X receipt |
| Independent finish review and bounded final verdict PASS, reported by parent task | **PASS for reviewed source fixes only**; no native finish approval |
| Minimum desk height 340 pixels, maximum 450 × 480 and result recap 62 pixels | Source-defined dimensions, not rendered measurements |
| User-approved three-panel concept illustration | Narrative visual direction; not a literal gameplay screen, native capture or shipped custom avatar |
| Native screenshots and rendered visual QA | None supplied; **HOLD** |
| Native listening, audio alignment, four real clients and phone interaction | **HOLD** pending actual supported-client evidence |
| World deployment, public video, DoraHacks and X | Not established by this packet; require actual public URLs/receipts |

The parent reports the bounded rhythm verdict PASS after midpoint clock synchronization, 80-millisecond note retries and acknowledgement grace, note-only pending-intent replacement, the 3.5-second card gate, battle feedback and HP-loss-triggered pulse/chime were reviewed. The initial height finding was retracted because its arithmetic was incorrect; do not record it as a real defect or a source fix. This verdict covers the reviewed findings. It does not establish that no other issue exists or that a rendered/native finish review passed.

The renderer uses actual canvas pixels and the reported interactable safe area. Compact mode begins below 496 pixels of safe height; the desk retains 46-pixel primary controls and a 40-pixel sound toggle, and rejects insufficient space below a 280-pixel width or 340-pixel height. The lane field contracts from 88 to 40 pixels. The two card rows appear for 3.5 seconds, then are replaced by the instrument; this sequential composition is the implemented mobile accommodation. Default SDK sans typography is intentional at this source scope. Test the actual interpretation of insets, long genre names, turn recap wrapping, compact lobby, spectator controls and reconnect feedback before asserting usable mobile layout.

Six seven-second rehearsals accelerate from 80 to 130 BPM. Four C/E/G notes begin at two seconds; three correct notes count as a success, and paired successes grant harmony. Five possible 12-second duel turns begin with RIFF / REST / AMP / CHORD selection, then notes at five seconds. Notes can add up to four crew attack to a funded attack/combo. Gold architecture, pink/lime stage accents, written crew signs and sphere speaker cones extend the geometry. The approved narrative image guides this direction without prescribing its three-panel composition.

`decentraland/scripts/generate-arena-audio.mjs` deterministically creates six original seven-second grooves and a 0.65-second resolution chord using synthesis, with no downloaded recording or sample. Output is mono 22,050 Hz, 16-bit PCM WAV. Sound starts off; enabling it plays the source at volume 0.4 and seeks to estimated phase age. Duel grooves begin at three seconds, so their two-second lead-in aligns with the first note target at five seconds. HP loss triggers an 800-millisecond geometry pulse and the chord if enabled. Generator inspection does not establish audible quality, exact client synchronization or successful playback.

In a supported Decentraland client, capture lobby, rehearsal, card selection, battle notes and result on desktop and phone with the visible viewport recorded. Confirm four distinct visitors, both crews, note timing/acknowledgements, duplicate-note rejection, card locking at 3.5 seconds, shared-budget failure, turn recap, draw/cancellation and reconnect behavior. Listen to mute/unmute, late-entry seeking, tempo progression, battle note alignment and the HP-loss chord; verify shield-only absorption does not trigger the pulse. The native run must verify its own state and identity evidence; fabricated players or a browser reconstruction cannot satisfy it. These remain runtime HOLD items, not a request to redo the completed bounded source review.

The `.impeccable/design.json` file extends DESIGN.md rather than duplicating its token primitives. Its HTML specimens are explicitly **documentation translations**, with browser-only focus hints for inspecting the documentation. They are not ReactEcs components, native captures, accessibility certification or proof of an imported SDK feature. Synthesized tonal ramps are documentation metadata, not shipping colors.

No shipping rasters occur in the reviewed arena scene source. Geometry and text are native primitives, and custom avatars were not delivered. The parent records the approved generated concept as narrative reference only; this documentation pass neither turns it into a native screenshot nor invents a provenance receipt. This does not audit unrelated preserved assets elsewhere in the repository.

Future changes should update this scoped system from final source after corrections and retain the boundaries above. Publication claims require a World entity receipt, usable public demo/video, DoraHacks confirmation and an actual X post receipt under the project's publication workflow.
